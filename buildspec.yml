version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22

  build:
    commands:
      - npm ci
      - npm run build

  post_build:
    commands:
      - aws s3 sync dist/ s3://${DEPLOY_BUCKET}/ --delete

artifacts:
  files:
    - '**/*'
  base-directory: dist
